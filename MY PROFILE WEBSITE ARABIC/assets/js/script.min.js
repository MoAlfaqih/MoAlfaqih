!(function () {
  "use strict";
  var e = document.querySelector("#mainNav");
  if (e) {
    var o = e.querySelector(".navbar-collapse");
    if (o) {
      var t = new bootstrap.Collapse(o, { toggle: !1 }),
        n = o.querySelectorAll("a");
      for (var r of n)
        r.addEventListener("click", function (e) {
          t.hide();
        });
    }
    var a = function () {
      (void 0 !== window.pageYOffset
        ? window.pageYOffset
        : (
            document.documentElement ||
            document.body.parentNode ||
            document.body
          ).scrollTop) > 100
        ? e.classList.add("navbar-shrink")
        : e.classList.remove("navbar-shrink");
    };
    a(), document.addEventListener("scroll", a);
    var d = document.querySelectorAll(".portfolio-modal");
    for (var c of d)
      c.addEventListener("shown.bs.modal", function (o) {
        e.classList.add("d-none");
      }),
        c.addEventListener("hidden.bs.modal", function (o) {
          e.classList.remove("d-none");
        });
  }
})(),
  document.querySelectorAll('a[href^="#"]').forEach((e) => {
    e.addEventListener("click", function (e) {
      var o = this.getAttribute("href"),
        t = document.querySelector(o);
      t && (e.preventDefault(), window.scrollTo(0, t.offsetTop - 54));
    });
  });

const myProfileWebsite = "Mo Alfaqih to GitHub";
